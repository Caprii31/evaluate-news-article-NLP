import { checkIfURL } from "../src/client/js/checker"

describe("check if URL is valid", () => {
     
    test("Testing the checkIfURL() function", () => {
          
           expect(checkIfURL).toBeDefined();
})});