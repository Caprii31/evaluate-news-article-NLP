import { passURL } from "../src/client/js/mainPlayer"

describe("post user URL and get response to update UI", () => {
     
    test("Testing the passURl() function", () => {
          
           expect(passURL).toBeDefined();
})});